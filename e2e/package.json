{
  "name": "@hammer/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end smoke: drives a real Host + players through a real match against a running server",
  "scripts": {
    "test": "tsx smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hammer/shared": "workspace:*",
    "colyseus.js": "^0.15.26"
  },
  "devDependencies": {
    "@types/node": "^22.8.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
